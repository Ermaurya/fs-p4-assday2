import React from 'react'

const Assa = () => {
  return (
    <div className='border-main'>
      <h1>Hellow Geekster</h1>
     <p>tha following all topic</p>
     <a href='geekster.in'>tha web</a>
     <p>The children are optional, and you can pass as many as you need (the example above has three children). This code will display </p>
     <a href='geekster.in'>HTML COVER</a>
     <p>The children are optional, and you can pass as many as you need (the example above has three children). This code will display <br/> 
     The children are optional, and you can pass as many as you need (the example above has three children). This code will display </p>
     <a href='geekster.in'>HTML COVER</a>
     <p>The children are optional, and you can pass as many as you need (the example above has three children). This code will display <br/>
    The children are optional, and you can pass as many as you need (the example above has three children). This code will display </p>
     
    </div>
  )
}

export default Assa
